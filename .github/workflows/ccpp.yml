name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    services:
      irods:
        image: docker://wsinpg/ub-12.04-irods-4.1:latest
        ports:
          - 1247:1247

    steps:
    - uses: actions/checkout@v1
    - name: autoreconf
      run: autoreconf -fi
