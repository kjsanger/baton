name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - uses: docker://wsinpg/ub-12.04-irods-4.1:latest
    - name: autoreconf
      run: autoreconf -fi
